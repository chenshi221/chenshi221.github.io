
**空间对齐（spatially-aligned）**：
- 指输入图像与输出图像在空间结构上具有一一对应关系，即像素位置之间保持对齐。
- 常用于保留图像原始结构的任务，如：
  - **Canny 边缘图**：提取图像边缘信息，边缘位置与原图结构对齐。
  - **深度图引导生成**：输出图像中的每个像素表示对应位置的深度值。
  - **语义分割、图像修复、超分辨率** 等也通常属于此类。